# rtfcluster-migration-app

Add the following properties as arguments

**mule.env** - local as default

**enc.key** - 16 char encryption key

**anypoint.user.pass** - encrypted user password (Anypoint credential)
